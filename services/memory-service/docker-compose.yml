services:
  memory-service:
    build: .
    ports:
      - "8100:8000"
    volumes:
      - ./data:/app/data
    environment:
      - EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
      - DEVICE=cpu
    restart: unless-stopped
